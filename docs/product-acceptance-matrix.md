# Numen 完整产品验收矩阵

> 状态基于 2026-08-19 的自动测试、隔离 Forge 实测与正式 Mohist 冒烟。`待正式复测` 表示代码、构件和隔离验证已完成，但正式服仍按要求冻结，尚未安装并加载最终候选。

| 能力 | 自动回归 | 隔离服实测 | 生产验收 | 通过标准 |
|---|---:|---:|---:|---|
| 真实 ServerPlayer 身体/持久身份 | 已通过 | 已通过 | 既有通过 | 原生物理、拾取、背包、存档、玩家列表可见 |
| MCP 鉴权/租约/限流 | 已通过 | 已通过 | 既有通过 | 未授权拒绝；单写租约；读取并发 |
| 同步感知（身体/世界/实体/视野） | 已通过 | 已通过 | 既有通过 | 返回结构化真实状态，无 queued 空包 |
| 延迟感知（scan_blocks/locate） | API 157 项通过 | `qN` 立即 `queued→done`，真实坐标 | 正式 `q33→done` 已通过；最新竞态修复待重启 | 派发后立即轮询不出现 unknown；终态保留真实结果 |
| 短动作（equip/interact/drop/eat/transfer） | 已通过 | equip `tN→failed` 业务原因 | 正式 `equip_item t1→failed` 已通过 | 返回 tN；终态有结构化成功/失败，不丢回调 |
| 长动作（goto/mine/build/combat/collect） | 已通过 | `mine t1→done` 真实破坏/拾取 | 待最新 JAR 复测 | 可轮询、可叫停、终态保留、实际世界核验 |
| 挖掘锁定/表面距离/侧向目标 | Core 248 项通过 | 侧向近距 stone 原地直接挖 | 正式工作台 `t3→done`，库存 `0→1` | 不再快挖完换目标；近距侧向目标不绕去远处 |
| NO_PATH 无进度收敛 | 已通过 | 局部失败预算已构建 | 待生产重启 | 同一区域连续无进展有限次后返回结构化 NO_PATH，不撞完 64 个候选 |
| 挖矿掉落拾取 | 已通过 | `gathered=1/1`；圆石库存 `0→1` | 工作台 `gathered=1/1`、库存 `0→1`、无遗留实体 | 原生 `playerTouch` 检查 pickupDelay/背包/事件；掉落不遗留 |
| 不足两槽时主动丢弃腾格 | Core 255 项通过 | 满 36 槽丢砾石，5 秒后保持 35 槽且真实掉落实体仍在 | 正式 35/36 时反复丢木板/木棍又捡回；已修为丢弃前空槽<2均长冷却，待重启 | 为两槽采矿门腾格时不在下一轮前捡回；已有≥2空槽则保持原版短冷却 |
| 采集生命/掉落容量保险丝 | Core 252 项通过 | 6 HP、满 36 槽、仅 1 空槽三场景均在破坏前失败，目标方块未变 | 正式 0/1 空槽均在破坏前拒绝；2 槽策略待重启 | 累计显著掉血或濒死立即 HAZARD；无同类堆栈时预留“路径副产物+目标掉落”两槽 |
| 导航停滞/梯子停止 | API 158 / Core 254 项通过 | 梯子停止策略字节码验证；隔离图无现成竖井 | 正式加载 `032f2caa…` 后，执行中 `goto t3` 叫停；回执 Y=19.205，身体连续 10 秒稳定 Y=19.360、HP 22；`mine t8` 返回真实 BOXED_IN | 200 tick 无移动/挖掘进展自动 BOXED_IN；停止时在可攀爬方块上保持原版潜行刹车 |
| 原版破坏提交真实性 | Core 254 项通过 | 待新候选隔离/正式验证 | 正式 `t11` 对 `(51,6,202)` 误记 16 次 mined_positions，但方块未变、粗铁仍 0；已修为世界方块类型变化后才 BROKE_TARGET | 插件/事件取消 STOP 时返回 BREAK_REJECTED，不伪造挖掘进度、不无限重试同一块 |
| 自动防卫保护生产工具 | 已通过 | 策略测试通过 | 待生产重启 | 镐/锹/锄不作为自动战斗武器消耗 |
| 制作/工作台 | Python+Java 已通过 | 木板→木棍、工作台→铁镐实服通过 | 已有通过 | 材料不足会调整计划，不盲重试 |
| 容器/熔炼复杂链 | 工具回归通过 | 完整通过；含主机重启恢复 | 待正式复测 | 放炉→填料/燃料→等待→取锭→库存核验 |
| 死亡/重启/checkpoint 恢复 | Python 110 项通过 | 旧实服部分验证 | Turn `c759a451…` 在 `t6` 中断；新 ActiveAgent 接回失败并重试 `t7→done` | 无静默、无事件重放、动作不重复破坏 |
| 自然语言目标 C | Python 113 项通过 | 15 铁→15 铁锭完整通过 | 待正式复测 | 一句话自主完成并聊天汇报，不需玩家追问 |
| Forge 服务端兼容 | Core 251 / API 157 项通过 | Forge 1.20.1 新候选通过 | 待重启 | 客户端不装 Numen 仍可加入 |
| Mohist/插件混合兼容 | 构建通过 | 既有生产运行 | 待最新 JAR 重启 | 插件与 Forge 工具链均正常；聊天触发可靠 |
| 投影蓝图桥接 | 设计完成 | 未实现 | 非当前 P0 | 客户端可选桥接，服务端不装投影模组，分批施工 |

## 2026-08-19 本轮真实验收记录

### 正式 Mohist（当前运行中的 `6ce47bda…`）

- 正式 JVM PID `26776`，游戏端口 `21294`、MCP `25567`；
- `scan_blocks q33 → done`，结构化返回 1659 个深板岩命中；
- `equip_item t1 → failed`，保留明确业务原因：背包没有铁镐；
- 测试工作台经原生左键破坏后自动回到背包，证明 ServerPlayer 基础掉落/拾取链有效；
- `mine` 暴露三处新问题：侧向近距目标被跳过、封闭候选逐个 NO_PATH、坑底掉落不触发假玩家碰撞拾取。

### 隔离 Forge（最新候选）

- 延迟查询派发后立即轮询：`q1 queued → done`，不再出现 `unknown task_id` 竞态；
- Actor 在生存模式、手持下界合金镐，从侧向近距石头执行 `mine t1`；
- 终态：`done`、`mined_positions=[-320,68,176]`、`gathered=1/1`；
- 实际背包：圆石 `0→1`，三把下界合金镐均保留；
- 隔离测试结束后已停止服务，并恢复安全策略（移除临时 `DESTRUCTIVE`）。

### 正式 Mohist（已加载 `4d48ca01…`）

- 新 JVM PID `10268`，创建于 `09:05:49`；启动日志于 `09:06:56` 显示 MCP ready；
- `scan_blocks q53→done`，立即轮询未出现 unknown；`equip_item t1→failed` 保留真实业务原因；
- 可逆工作台测试：放置后库存 `1→0`，`mine t3→done`、`gathered=1/1`，坐标变空气、库存 `0→1`，6 格内无遗留物品实体；
- 自然语言 Turn `c759a451-8aaf-42f4-9e5c-5557959ef788` 中断恢复：前台大脑在 `build t6` 运行时被终止；新 ActiveAgent 读取同一 checkpoint，接回 `t6 failed` 后重试 `t7→done`；
- 模型自主完成木板/工作台/木镐链，并在木镐不能采铁后选择石镐；但 36 个标准背包槽全满，`drop_items t11/t12` 的物品在原版短拾取延迟后被自己捡回，石镐无法落包。本轮因此停止，未误挖铁矿；
- 修复为：仅满背包场景把本次主动掉落实体的原生拾取冷却延长到 1200 tick；非满背包仍保留原版递交语义。隔离真世界验证：满 36 槽丢砾石，5 秒后仍为 35 槽且砾石实体留在地面；测试后恢复 Actor 数据、隔离 JAR 和安全策略。

### 正式 Mohist（已加载 `17554ed5…`）

- 原 Turn `c759a451…` 从满背包 checkpoint 继续：低价值物品成功保持丢弃，石镐合成成功（圆石 `15→12`、木棍 `4→2`），`equip t2→done`；
- `mine t3` 在矿井施工式导航中从 Y=-11 移动到 Y=3，并用圆石搭路；石镐始终保留，自动防卫没有拿镐作武器；
- 路途中 HP `22→14→6`，进度仍 `0/15`。按无人监督异常规则立即 `task_stop`；终态 `stopped`、`gathered=0/15`；附近发现 Goety spider_web，Web Spider 本体退至约 14 格，且地形图有大量 3 格以上落差，伤害源可能是怪物与坠落共同作用，未伪装成已证实的单一因果；
- `t3` 记录 `mined_positions=[-129,4,-105]` 但无粗铁，标准 36 格又被石镐占满，暴露“满背包仍破坏新掉落矿物”缺口；
- 新增两道前置保险丝：当前 HP≤8 或本任务累计损失≥6 HP 时返回 HAZARD；背包无空槽且无目标掉落可叠加堆栈时返回 NO_MATERIAL。两项隔离 Forge 实测均在破坏前 `failed`，目标 stone 前后不变、`mined_positions=[]`；测试后恢复 Actor 数据/JAR/安全策略并停服。

### 正式 Mohist（已加载 `72689bb4…`）

- 新 JVM PID `23340`，创建于 `10:38:24`；MCP 于 `10:39:30` ready。服务器重启把身体恢复到地表 `22/22 HP`，明确标记为重启事故恢复，不冒充模型自主求生；
- 同一 Turn `c759a451…` 自动接回。采矿容量保险丝在正式服多次返回 `mined_positions=[]`，没有再破坏无法接收掉落的铁矿；模型自主丢弃木镐、骨头、去皮原木、缠根泥等低价值物；
- 单空槽会被施工副产物重新占据：圆石拆成 `10+1` 两格，后又拾得 `barrel`、`diorite`。因此“只腾一格”无法保证目标粗铁有位置；新策略改为无目标同类堆栈时至少预留两格。隔离 Forge 35/36 槽实测 `mine stone→failed`、目标 stone 不变、`mined_positions=[]`；
- `mine t10/t13` 在 `(53,约2,179)` 梯子格内长时间 `0/15`，位置只在同格微动，原执行器的重规划/单移动超时未及时收敛。新增采矿导航 200 tick liveness 保险丝，连续无跨步且无真实挖掘时返回 `BOXED_IN`；
- 停止 t13 后模型派发 `goto t14→(51,-14,172)`。task_stop 回执记录 `final_y=-13.15`，但随后身体按梯子原版重力继续滑至 Y=-42（HP 仍 22/22、落地且无敌对实体），终态快照与最终物理落点不一致。根因是清理只清水平输入并强制松潜行；修复为停止/挂起时若 `onClimbable()` 则保持原版潜行刹车，平地才松开。ActiveAgent 与身体任务均已停止，checkpoint 保留 t14。

### 正式 Mohist（已加载 `032f2caa…`）

- 新 JVM PID `21256`，创建于 `11:32:14`；MCP 于 `11:33:37` ready；同一 Turn checkpoint 保留并自然消费重启后已清理的 t14；
- 梯子真实回归通过：先在 Y=-35.261 悬停 8 秒零滑移；再执行 `goto t3→Y=40` 并在途中明确 task_stop，回执 `final_y=19.205`，身体随后连续 10 秒稳定在 `Y=19.360`，仅服务端 tick 快照差约 0.154 格，HP 始终 22/22；
- 模型自主丢弃砾石后背包达到 34/36，满足两槽策略；`mine t8` 无矿物损失并返回结构化 BOXED_IN，证明导航停滞不再无限运行；
- 恢复后 `mine t11` 直接处理头顶 `iron_ore`，位置不动且进度 0/15。停止终态却包含同一 `(51,6,202)` 16 次 mined_positions；现场方块仍为 `minecraft:iron_ore`，粗铁仍 0。根因是 `BlockDigger` 发送原版 STOP 后未检查世界状态便返回 BROKE_TARGET；领地/事件取消会导致假成功循环；
- t11 停止、大脑停止后，监督日志最后记录身体在 `(52.59,-2,215.55)` 降至 `1/22 HP`，随后 `12:07:09` 服务器事件确认 yachiyo 死亡并重生到地表。此前同一区域持续观察到苦力怕，但缺少最终伤害来源日志，故只记录强相关、不宣称单一因果；重生后背包物品保留、HP 22/22、16 格内无敌对实体；
- 修复：START instamine、creative 与 STOP 三条路径都比较提交前后方块类型；未变化返回 `BREAK_REJECTED`。Mine/Build 均结构化 HAZARD 终止，不记录挖掘进度。Core clean 全量 254/254，新候选待重启验证。

### 正式 Mohist（已加载 `7cbfeac1…`）

- 新 JVM PID `4912`，创建于 `12:16:20`；MCP 于 `12:17:40` ready；重生后身体地表 22/22，背包 34/36；
- 受控 `mine count=1` 沿路拾到 dirt，使背包回到 35/36；两槽门正确在破坏前拒绝，`mined_positions=[]`、HP 22/22；未触及目标，故该次不能充当 BREAK_REJECTED 正式验证；
- 同一 Turn 自然接回旧 t11 后，模型依次丢弃 spruce_planks、stick 并重试采矿；每次 drop 回执 done，但下一轮仍只有1空槽。根因是旧长拾取冷却仅覆盖 36/36，35/36 时物品使用原版短冷却并在模型下一轮前被自己捡回；
- 修复为按丢弃前普通空槽数决定：0或1格时使用1200 tick冷却，为两槽采矿安全门稳定腾格；已有≥2格时保留原版短冷却。TDD红灯缺少新策略方法，修复后 clean 全量255/255。

### C 盘隔离 Forge 完整目标 C 验收（最新候选）

- 将高速隔离服迁移至 `C:\NumenLab\forge-light`；与 D 盘完整基线核对 221 个文件、194,398,195 字节，无缺失、额外或大小差异，关键配置、世界与 JAR SHA 一致；D 盘副本保留作回滚基线。
- 纯 Forge 复现“铁块已破坏、粗铁掉落在 1.85 格外、任务仍 0/15”的目标掉落导航死锁。根因是待捡目标掉落期间，近处可挖矿每 tick 中断并重建导航。修复后真实 `mine count=1` 中 Actor 主动接近掉落、粗铁 `0→1`、终态 `done`、无遗留实体。
- 敌对链实测 Actor 被骷髅射击，HP 从 20 持续下降直至死亡。根因是怪物防卫优先级 5 永久压住低血进食 4。低血恢复调至 5.5 后，在骷髅压力下两次主动吃金苹果，饥饿恢复到 20、HP 恢复到 20，未再次死亡；普通饥饿仍为优先级 3。
- 显式近战增加武器前置。仅携带镐、方块和食物时，真实 `melee_attack t1→failed/WRONG_TOOL`，命中 0、身体位置不变、镐未被使用；无武器应逃跑。
- 对含“熔炼/烧成”等意图的任务注入强制加工前置。最终验收 Turn `5432b8ba-c420-4275-8667-a5bd58405594` 的首个资源动作先采 12 石料和 4 桦木，随后自主完成木板、工作台、炉子与燃料准备，之后才采目标铁矿。
- 同一 Turn 自主完成 `mine t5`：`gathered 15/15 iron_ore+1`；随后完成 `mine t6`：`gathered 15/15 coal_ore+1`，返回已放置炉子并通过真实 `FurnaceMenu` 放入 15 粗铁和 15 煤。
- 熔炼期间主机发生真实重启。重启后未新建 Turn、未编辑 checkpoint、未人工补物品或动作；隔离服从原世界存档恢复，同一 checkpoint 从 `tool_completed/close_gui` 接续。炉内状态核验为输入 0、燃料煤 13、输出铁锭 15，证明停机前容器状态持久化正常。
- 恢复进程正常取出输出槽 15 铁锭，关闭界面并调用 `get_self_status` 核验：Actor HP 20/20、三把下界合金镐保留、背包 slot 5 为 `iron_ingot x15`、粗铁 0。最终回执：`已完成：挖了15个铁矿石，用15个煤烧成15个铁锭，全部留在背包里（slot 5）。`
- 最终 checkpoint 自动删除；恢复进程 exit 0；行为验收 JAR 与隔离部署 SHA-256 均为 `3fbcb5a64e109f4ccfa88de635f2c351f0add614bae4d721cc3e522231f7883f`。验收后 clean 重建的发布构件 SHA-256 为 `8500c4a518285bf499166efd8a2a9e12ff0b28cb1f44461d79d22fea38cf57d3`；两者 325 个 `.class` 的路径与逐类 SHA-256 完全一致（0 新增、0 缺失、0 字节码变化），差异仅来自 JAR 重打包元数据。
- 本轮还经历一次 SenseNova HTTP 读取超时；两次恢复均复用同一 Turn 和 checkpoint，没有重复已完成的世界副作用。该链同时验证了自然语言规划、采集、制作、放置、导航、容器、熔炼、输出提取、结果保留、模型超时恢复和服务器重启恢复。

## 当前 P0 生产验收

正式服需要加载本轮 clean 构建的最终候选 SHA-256
`8500c4a518285bf499166efd8a2a9e12ff0b28cb1f44461d79d22fea38cf57d3`，然后只做最终生产复测：

1. 确认正式 JVM 加载的 JAR SHA 与磁盘候选一致，MCP `25567` 正常；
2. 恢复既有正式 Turn `c759a451-8aaf-42f4-9e5c-5557959ef788`，不得新建替代任务或人工完成步骤；
3. 核验目标掉落优先导航、低血进食、无武器近战拒绝在 Mohist 插件环境中没有兼容性退化；
4. 继续并完成“挖 15 个铁并烧成铁锭”，核对本次目标相关的 raw iron、工具、地面掉落、炉子与 iron_ingot 增量；
5. 最终成果必须留在背包，并在游戏聊天返回与库存一致的完成回执。

只有正式服以上各项全部通过，才标记“目标 C 生产验收完成”。
